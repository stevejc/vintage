<h1>User Show</h1>

<table class="table">
  <tr>
    <td><b>First Name:</b></td>
    <td>
      <%= @user.first_name %>
    </td>
  </tr> 

  <tr>
    <td><b>Last Name:</b></td>
    <td>
      <%= @user.last_name %>
    </td>
  </tr>
  
  <tr>
    <td><b>Email:</b></td>
    <td>
      <%= @user.email %>
    </td>
  </tr>
   
</table>

<%= link_to 'Back', :back, :class => 'btn' %>
<%= link_to 'Edit', edit_user_path(@user), :class => 'btn' %>
<%= link_to 'Change Password', password_path, :class => 'btn' %>
<%= link_to 'Add Shop', root_path, :class => 'btn' %>